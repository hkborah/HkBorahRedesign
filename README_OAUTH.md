# OAuth Note
The Google OAuth error `401: invalid_client` happens because the `VITE_GOOGLE_CLIENT_ID` environment variable is either missing or invalid. I need to make sure the user configures their own OAuth Client ID.
